{
  "output.topic.name": "output1",
  "match": {},
  "transform": [
    {
      // Note: this transformation is for avro input
      "operation": "shift",
      "spec": {
        "event": "[].transformed_event"
      }
    }
  ]
}
